<?xml version="1.0" encoding="utf-8"?><!--
   Sample data extraction rules file; uncomment and customize as necessary.
   See https://developer.android.com/about/versions/12/backup-restore#xml-changes
   for details.
-->
<data-extraction-rules>
    <!-- Scanned-part photos (img_*.jpg in filesDir) are excluded from both cloud backup and
         device-to-device transfer; the Room database and SharedPreferences are backed up by
         default since nothing else lives in filesDir. -->
    <cloud-backup>
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>